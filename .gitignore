# Dependencies
node_modules/

# Build output
dist/
*.tsbuildinfo

# Local environment (copy .env.example)
.env
.env.local
.env.*.local

# Generated by pnpm db:seed — run seed before k6 load tests
k6/wallets.json

# Test / tool caches and reports
coverage/
.nyc_output/
.vitest/
*.log
npm-debug.log*
pnpm-debug.log*
.eslintcache

# OS / editor
.DS_Store
Thumbs.db
*.swp
*~
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json.example

# Docker (named volumes live outside the repo; ignore local overrides)
docker-compose.override.yml

# Dev-only uploads (not needed to run the project)
claude-project-upload.zip
*-project-upload.zip

# Cursor (local only)
.cursor/
